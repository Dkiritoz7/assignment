#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a=1;
    while(1)
    {
        system("pause");
    }
    return 0;
}
//反思循环体里面可以使用getc( )或者system(“pause”)实现每按一次任意键，循环执行一次
//同时循环到最后会变成0和负数，应该是超过了int的最大值